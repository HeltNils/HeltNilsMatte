<!DOCTYPE html>
<html lang="no">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Helt Nils lekser Nivå 2</title>
  <style>
    :root {
      --cell: 35px;
      --grid-line: rgba(0, 0, 0, 0.14);
      --paper: #f8f7f2;
      --ink: #111;
      --line: #111;
    }

    body {
      margin: 0;
      background: #ececec;
      display: flex;
      justify-content: center;
      padding: 20px;
      font-family: "Comic Sans MS", "Trebuchet MS", sans-serif;
    }

    .page {
      width: 1180px;
      min-height: 930px;
      box-sizing: border-box;
      padding: 56px 70px 60px;
      background-color: var(--paper);
      background-image:
        linear-gradient(to right, var(--grid-line) 1px, transparent 1px),
        linear-gradient(to bottom, var(--grid-line) 1px, transparent 1px);
      background-size: var(--cell) var(--cell);
      position: relative;
    }

    .topbar {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 20px;
      margin-bottom: 54px;
      flex-wrap: wrap;
    }

    .controls {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
    }

    button {
      border: none;
      border-radius: 10px;
      padding: 10px 16px;
      font-size: 1rem;
      cursor: pointer;
      background: #333;
      color: white;
    }

    button:hover {
      opacity: 0.9;
    }

    .ruteord {
      display: inline-grid;
      grid-auto-flow: column;
      grid-auto-columns: var(--cell);
      align-items: center;
      justify-items: center;
      height: var(--cell);
      padding: 0 12px;
      border-radius: 18px;
      font-size: 2rem;
      line-height: 1;
      letter-spacing: 0;
      white-space: nowrap;
    }

    .ruteord span {
      width: var(--cell);
      height: var(--cell);
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .title-block {
      background: #b8c9ef;
      margin-bottom: 18px;
    }

    .symbols {
      display: inline-grid;
      grid-auto-flow: column;
      grid-auto-columns: var(--cell);
      align-items: center;
      justify-items: center;
      font-size: 2rem;
      line-height: 1;
    }

    .symbols span {
      width: var(--cell);
      height: var(--cell);
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      column-gap: 115px;
      row-gap: 95px;
      margin-top: 8px;
    }

    .section-title {
      margin-bottom: 46px;
    }

    .green { background: #d7eb7d; }
    .purple { background: #d48be9; }
    .pink { background: #f2a29a; }
    .yellow { background: #ece86b; }

    .problem-wrap {
      min-height: 140px;
    }

    .vertical-problem {
      display: inline-grid;
      justify-items: center;
      align-items: center;
      color: var(--ink);
      font-size: 2rem;
      line-height: 1;
      margin-left: calc(var(--cell) * 1.5);
    }

    .cell {
      width: var(--cell);
      height: var(--cell);
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .math-line,
    .answer-line {
      border-top: 2px solid var(--line);
      height: 0;
      width: 100%;
    }

    .horizontal-problem {
      display: inline-grid;
      align-items: center;
      justify-items: center;
      font-size: 2rem;
      line-height: 1;
      color: var(--ink);
      margin-left: calc(var(--cell) * 0.8);
    }

    .h-answer-line {
      border-top: 2px solid var(--line);
      height: 0;
      width: 100%;
      align-self: center;
    }

    .footer {
      margin-top: 65px;
      font-size: 1rem;
      color: #444;
    }

    @media print {
      body {
        background: white;
        padding: 0;
      }

      .controls {
        display: none;
      }

      .page {
        margin: 0;
      }
    }
  </style>
</head>
<body>
  <div class="page">
    <div class="topbar">
      <div>
        <div id="mainTitle" class="ruteord title-block"></div>
        <div id="symbolRow" class="symbols"></div>
      </div>

      <div class="controls">
        <button onclick="lagOppgaver()">Lag nye oppgaver</button>
        <button onclick="window.print()">Skriv ut</button>
      </div>
    </div>

    <div class="grid">
      <div class="section">
        <div id="titleAddisjon" class="ruteord section-title green"></div>
        <div class="problem-wrap" id="addisjon"></div>
      </div>

      <div class="section">
        <div id="titleSubtraksjon" class="ruteord section-title purple"></div>
        <div class="problem-wrap" id="subtraksjon"></div>
      </div>

      <div class="section">
        <div id="titleMultiplikasjon" class="ruteord section-title pink"></div>
        <div class="problem-wrap" id="multiplikasjon"></div>
      </div>

      <div class="section">
        <div id="titleDivisjon" class="ruteord section-title yellow"></div>
        <div class="problem-wrap" id="divisjon"></div>
      </div>
    </div>

    <div class="footer">
      Navn: ____________________________ &nbsp;&nbsp;&nbsp; Dato: __________________
    </div>
  </div>

  <script>
    function tilfeldigTall(min, maks) {
      return Math.floor(Math.random() * (maks - min + 1)) + min;
    }

    function settRuteord(id, tekst) {
      const el = document.getElementById(id);
      el.innerHTML = "";
      for (const tegn of tekst) {
        const span = document.createElement("span");
        span.innerHTML = tegn === " " ? "&nbsp;" : tegn;
        el.appendChild(span);
      }
    }

    function lagVertikalOppgave(tall1, operator, tall2) {
      const a = String(tall1).split("");
      const b = String(tall2).split("");
      const bredde = Math.max(a.length, b.length) + 1; // plass til operator

      let html = `
        <div class="vertical-problem" style="
          grid-template-columns: repeat(${bredde}, var(--cell));
          grid-template-rows:
            var(--cell)
            var(--cell)
            var(--cell)
            calc(var(--cell) * 0.6)
            calc(var(--cell) * 0.6);
        ">
      `;

      // Øverste tall
      html += `<div class="cell"></div>`;
      for (let i = 0; i < bredde - 1 - a.length; i++) {
        html += `<div class="cell"></div>`;
      }
      for (const siffer of a) {
        html += `<div class="cell">${siffer}</div>`;
      }

      // Nederste tall med operator
      html += `<div class="cell">${operator}</div>`;
      for (let i = 0; i < bredde - 1 - b.length; i++) {
        html += `<div class="cell"></div>`;
      }
      for (const siffer of b) {
        html += `<div class="cell">${siffer}</div>`;
      }

      // Strek
      html += `<div class="math-line" style="grid-column: 1 / span ${bredde};"></div>`;

      // Svarrad
      html += `<div class="cell">=</div>`;
      for (let i = 1; i < bredde; i++) {
        html += `<div class="cell"></div>`;
      }

      // Svarstrek
      html += `<div class="answer-line" style="grid-column: 1 / span ${bredde};"></div>`;

      html += `</div>`;
      return html;
    }

    function lagHorisontalOppgave(tall1, operator, tall2, strekLengde = 4) {
      const tegn = `${tall1}${operator}${tall2}=`.split("");
      const cols = Math.max(14, tegn.length + strekLengde + 1);

      let html = `
        <div class="horizontal-problem" style="
          grid-template-columns: repeat(${cols}, var(--cell));
          grid-template-rows: var(--cell) calc(var(--cell) * 0.6) calc(var(--cell) * 0.6);
        ">
      `;

      for (let i = 0; i < cols; i++) {
        if (i < tegn.length) {
          html += `<div class="cell">${tegn[i]}</div>`;
        } else {
          html += `<div class="cell"></div>`;
        }
      }

      for (let i = 0; i < cols; i++) {
        if (i >= tegn.length && i < tegn.length + strekLengde) {
          html += `<div class="h-answer-line"></div>`;
        } else {
          html += `<div></div>`;
        }
      }

      for (let i = 0; i < cols; i++) {
        if (i >= tegn.length && i < tegn.length + strekLengde) {
          html += `<div class="h-answer-line"></div>`;
        } else {
          html += `<div></div>`;
        }
      }

      html += `</div>`;
      return html;
    }

    function lagAddisjon() {
      const a = tilfeldigTall(100, 999);
      const b = tilfeldigTall(100, 999);
      document.getElementById("addisjon").innerHTML = lagVertikalOppgave(a, "+", b);
    }

    function lagSubtraksjon() {
      const a = tilfeldigTall(200, 999);
      const b = tilfeldigTall(100, a);
      document.getElementById("subtraksjon").innerHTML = lagVertikalOppgave(a, "-", b);
    }

    function lagMultiplikasjon() {
      const a = tilfeldigTall(100, 999);
      const b = tilfeldigTall(10, 99);
      document.getElementById("multiplikasjon").innerHTML = lagHorisontalOppgave(a, "·", b, 4);
    }

    function lagDivisjon() {
      const divisor = tilfeldigTall(10, 99);
      const svar = tilfeldigTall(10, 99);
      const dividend = divisor * svar;
      document.getElementById("divisjon").innerHTML = lagHorisontalOppgave(dividend, ":", divisor, 4);
    }

    function lagOppgaver() {
      lagAddisjon();
      lagSubtraksjon();
      lagMultiplikasjon();
      lagDivisjon();
    }

    settRuteord("mainTitle", "Helt Nils lekser Nivå 2");
    settRuteord("titleAddisjon", "Addisjon");
    settRuteord("titleSubtraksjon", "Subtraksjon");
    settRuteord("titleMultiplikasjon", "Multiplikasjon");
    settRuteord("titleDivisjon", "Divisjon");
    settRuteord("symbolRow", "123456789+-·:=");

    lagOppgaver();
  </script>
</body>
</html>